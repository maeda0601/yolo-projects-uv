[project]
name = "yolo-projects-uv"
version = "0.1.0"
description = "YOLO v8 GUI application for image collection, annotation, and real-time object detection"
requires-python = ">=3.8"
dependencies = [
    "opencv-python>=4.5.0",
    "pillow>=9.0.0",
    "pyyaml>=6.0",
    "ultralytics>=8.0.0",
]
